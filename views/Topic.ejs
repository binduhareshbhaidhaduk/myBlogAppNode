<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Topic Submission</title>
    <style>
        /* General Styles */
        *{
            box-sizing: border-box;
            margin: auto;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #1e1e2f;
            color: #f0f0f0;
            margin: 0;
            padding: 0;
            /* display: flex; */
            justify-content: center;
            align-items: flex-start;
        }
    
        /* Container for the form and topics */
        .container {
            background-color: #2e2e3e;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
            max-width: 700px;
            width: 90%;
            margin-top: 2rem;
        }
    
        /* Title and subtitle styling */
        .title, .subtitle {
            text-align: center;
            color: #ffffff;
            margin-bottom: 1rem;
        }
    
        .subtitle {
            font-size: 1.5rem;
            border-bottom: 2px solid #444;
            padding-bottom: 0.5rem;
            margin-top: 2rem;
        }
    
        /* Form Styling */
        .form-container {
            margin-bottom: 2rem;
        }
    
        .form {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
    
        label {
            font-size: 1.1rem;
            color: #ddd;
        }
    
        input[type="text"] {
            padding: 0.75rem 1rem;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            outline: none;
        }
    
        input[type="text"]:focus {
            box-shadow: 0 0 5px #6c63ff;
        }
    
        button {
            padding: 0.75rem 1.5rem;
            background-color: #6c63ff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
            transition: background-color 0.3s ease;
            align-self: flex-start;
        }
    
        button:hover {
            background-color: #5750d3;
        }
    
        /* Topics List Styling */
        .topics-container {
            margin-top: 1rem;
        }
    
        .topics-list {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }
    
        .topic-item {
            background-color: #3e3e5e;
            padding: 1rem;
            margin-bottom: 0.75rem;
            border-radius: 5px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: background-color 0.3s ease;
        }
    
        .topic-item:hover {
            background-color: #4e4e6e;
        }
    
        .topic-text {
            font-size: 1.1rem;
            color: #f0f0f0;
        }
    
        .remove-form {
            margin: 0;
            padding: 0px !important;
            width: 100px;
        }
    
        .remove-btn {
            background-color: #ff4d4d;
            border: none;
            padding: 0.5rem 1rem;
            color: #fff;
            border-radius: 5px;
            cursor: pointer;
            font-size: 0.9rem;
            transition: background-color 0.3s ease;
        }
    
        .remove-btn:hover {
            background-color: #e04343;
        }

       
        div ul.navbar-nav{
            list-style: none;
            margin: 0px;
            display: flex;
        }
    
        /* Header (Navbar) Styling */
        .navbar {
            background-color: #5750d3;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        li a.nav-link{
            display: flex !important;
            flex-wrap: wrap !important;
        }
    
        .navbar a.navbar-brand {
            color: #fff;
            font-size: 1.5rem;
            text-decoration: none;
        }
    
        .navbar a.nav-link {
            color: #f0f0f0;
            font-size: 1rem;
            text-decoration: none;
            padding: 0.5rem 1rem;
            transition: color 0.3s ease;
        }
    
        .navbar a.nav-link:hover {
            color: #6c63ff;
        }
    
        .navbar i {
            color: #f0f0f0;
            font-size: 1.2rem;
            margin-left: 1rem;
            cursor: pointer;
        }
    
        .navbar i:hover {
            color: #6c63ff;
        }
    
        .navbar-text {
            color: #f0f0f0;
            font-size: 1rem;
        }
    
        /* Responsive Design */
        @media (max-width: 600px) {
            .container {
                padding: 1.5rem;
            }
    
            .title {
                font-size: 1.5rem;
            }
    
            .subtitle {
                font-size: 1.2rem;
            }
    
            .form {
                gap: 0.75rem;
            }
    
            button, .remove-btn {
                width: 100%;
                text-align: center;
            }
    
            .topic-item {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5rem;
            }
    
            .navbar {
                flex-direction: column;
                align-items: flex-start;
            }
    
            .navbar a.nav-link {
                padding: 0.5rem 0;
            }
    
            .navbar i {
                margin: 0.5rem 0;
            }
        }
    </style>
    

</head>
<body>
    <%- include('partials/header') %>

    <div class="container">
        <h1 class="title">Submit a Topic</h1>
        
        <!-- Form Section -->
        <div class="form-container">
            <form action="/submit" method="post" class="form">
                <label for="topic">Enter Topic:</label>
                <input type="text" id="topic" name="topic" placeholder="Type your topic here..." required>
                <button type="submit" id="submit-btn">Submit</button>
            </form>
        </div>
        
        <!-- Topic List Section -->
        <div id="topic-display" class="topics-container">
            <h2 class="subtitle">Submitted Topics</h2>
            <ul class="topics-list">
                <% topics.forEach((t, index) => { %>
                    <li class="topic-item">
                        <span class="topic-text"><%= t.topic %></span>
                        <% if (t.owner.equals(user._id)) { %>
                            <form action="/remove/<%= t._id %>" method="post" class="remove-form">
                                <button type="submit" class="remove-btn">Remove</button>
                            </form>
                        <% } %>
                    </li>
                <% }); %>
            </ul>
        </div>
    </div>

    <script src="script.js"></script>
</body>
</html>
